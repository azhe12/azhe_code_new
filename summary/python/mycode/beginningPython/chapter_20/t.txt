//=============== #
用于函数宏的替换部分，#符号用作一个预处理运算符，可以把语言符号转化成字符串，该过程称为字符串化stringizing
#define DEF( x ) #x
char *str = DEF(w+t+f); // 结果就是字符串"w+t+f"
 

//=============== ##
用于函数宏的替换比分。把两个语言符号组合成一个语言符号。

#define DEF( x, y ) x##y

int a4 = 1024;
cout<<DEF(a,4)<<endl; // 打印结果就是 1024
